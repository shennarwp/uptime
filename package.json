{
  "private": true,
  "scripts": {
    "prepare": "husky",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "husky": "^9.1.7"
  },
  "lint-staged": {
    "*.{ts,tsx,js,jsx}":  [
      "eslint --cache --fix",
      "prettier --write"
    ],
    "*.{ts,js,css,md,html,json,yaml,yml}": "prettier --write"
  }
}
